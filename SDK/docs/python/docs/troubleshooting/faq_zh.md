# 常见问题（FAQ）

## 我需要在机器人上安装什么吗？
不需要。Python SDK 已随机器人镜像一并安装。

## 我可以修改 SDK 源码并重新构建吗？
可以。每个模块都包含 `source/` 目录，内含 `pyproject.toml` 与 CMake 配置，可用于从源码构建。

## 为什么我的脚本会与 Doly 服务冲突？
如果服务与脚本同时尝试访问同一硬件，会发生冲突。在直接控制硬件的脚本运行前，请先停止 `doly.service`。示例中展示了如何停止服务以避免冲突。