# 权限

某些模块会访问 Linux 设备节点（例如 `/dev/doly_lcd`）或 sysfs 条目。若模块在 root 下可用但在普通用户下不可用，通常是权限问题。系统已将 `doly` 用户加入自定义 `doly` 组。若你要添加新用户以访问这些设备，请按下述建议操作。

## 推荐做法

- 使用 **udev 规则** 为 Doly 设备分配组和权限
- 将你的用户添加到合适的组（通常为 `video`、`dialout`，或自定义的 `doly` 组）
