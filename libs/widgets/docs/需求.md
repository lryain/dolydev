# widgets
# 激活python虚拟环境
source /home/pi/dolydev/.venv/bin/activate

cd ~/dolydev/libs/widgets/python

分析这两个例子：
libs/widgets/python/test_widget_service.py
libs/widgets/python/widget_clock_api_test.py
提炼出 widgets的全部细粒度api接口和使用方法，并编写 widgets 模块的skills技能文档到 .github/skills/widgets-skill.md，
然后ai agent可以根据这个skill方便的开发出各种功能的组合。
比如开源轻松实现不限于下面的功能：
1. 报时的同时显示时钟，并在3秒后自动隐藏
2. 启动一个30秒的倒计时，并在3秒后自动隐藏（在最后10秒会自动显示出来），并且在倒计时结束（通过订阅timer消息）时通过调用多利的 animation_system来执行一个动画
python3 /home/pi/dolydev/modules/animation_system/cli.py run_file /home/pi/dolydev/assets/config/animations/alarm.xml
这样就形成了一个完美的倒计时流程



