   cd libs/FaceReco/build
   ./vision_service

sudo pkill -f LiveFaceReco
cd /home/pi/dolydev/libslibs/FaceReco/build
./LiveFaceReco > LiveFaceReco.log 2>&1 &












## 更新代码
如果程序不是最新版本使用更新脚本，将代码更新到最新并重新部署
./scripts/update_and_redeploy.sh

## 首先注册人脸

1. 先停止 vision_service
sudo systemctl stop vision-service.service

2. 通过命令行注册人脸
cd ~/dolydev/libs/FaceReco/build
./LiveFaceReco register_face
执行后会进行人脸识别，识别到人脸之后会提示输入人名：推荐使用英文名字 输入后回车即可注册完成
然后Contro+C接收程序

3. 恢复 vision_service
sudo systemctl restart vision-service.service

4. 使用命令词测试
拍照，录像，看看我是谁（人脸识别）
修改人名:
libs/FaceReco/data/face_db.json 中的name
配置人物关系是在 config/face_reco_settings.yaml

