[build-system]
requires = ["setuptools>=42", "wheel", "pybind11>=2.6.0", "cmake>=3.16"]
build-backend = "setuptools.build_meta"

[project]
name = "SerialControl"
version = "1.0.0"

[tool.setuptools]
packages = []
